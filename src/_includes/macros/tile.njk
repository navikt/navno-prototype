{% macro tile(args) %}
 <div class="relative px-5 py-4 border rounded-lg border-slate-300 {{ args.theme }}">
  {{ args.icon | svgContents('max-w-16 h-auto w-[calc(8vw+1rem)]') | safe if args.icon }}
  <a class="flex-1 text-base font-semibold leading-snug after:absolute after:inset-0" href="#">{{ args.title }}</a>
  {% if args.kicker %}
    <p class="text-xs font-medium tracking-wider uppercase opacity-75">{{ args.kicker | safe }}</p>
  {% endif %}
  {# {% if args.content %}
    <p class="opacity-85">{{ args.content | safe }}</p>
  {% endif %} #}
</div>
{% endmacro %}
