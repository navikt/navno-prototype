{%- from "macros/button.njk" import button -%}

{% for block in product.contentBlocks %}

  {% if block.type === 'markdown' %}
    {% for item in block.data %}
      {{ item | markdown | safe }}
    {% endfor %}
  {% endif %}

  {% if block.type === 'actions' %}
  <div class="px-6 pt-5 pb-6 mt-4 mb-12 rounded-lg bg-deepblue-50">
    <p class="font-semibold">{{ block.title | safe }}</p>
    <div class="flex flex-col flex-wrap items-baseline gap-2 mt-2 md:flex-row">
      {% for item in block.data %}
        {{ button(
          {
            text: item.text,
            variant: item.variant,
            class: 'flex-1'
          }
        ) }}
      {% endfor %}
    </div>
  </div>
  {% endif %}
{% endfor %}
