---
type: 'Privatperson'
title: 'Forside'
description: 'Lenker til de viktigste områdene + utvalgte snarveier.'
tags: ['template']
order: 1
noJS: true
permalink: false
---
{%- extends 'base.njk' -%}

{%- block content -%}
  <main x-bind:inert="menuOpen || notifyOpen">
    <div>
      <div>
        <h2>Hva kan vi hjelpe deg med?</h2>
        <ul>
          {%- for area in areaList -%}
            <li>
              <a href="#">
                <span>{{ area.title | safe }}</span>
                {# {{ area.icon | svgContents("-z-10 right-0 h-full w-auto rounded-lg self-stretch absolute inset-y-0 pointer-events-none") | safe if area.icon }} #}
              </a>
            </li>
          {%- endfor -%}
        </ul>
      </div>
    </div>

    <div>
      <div>
        <h2>Snarveier</h2>
        <ul>
          {%- for shortcut in shortcutsList -%}
            <li>
              <a href="#">
                {# {{ shortcut.icon | svgContents("w-12 h-12 md:w-16 md:h-16") | safe if shortcut.icon }} #}
                <span>{{ shortcut.title | safe }}</span>
              </a>
            </li>
          {%- endfor -%}

        </ul>
      </div>
    </div>


    <div>
      <div>
        <h2>Søke og ettersende</h2>
        <ul>
          {%- for form in formsList -%}
            <li>
              <a href="{{ form.href or '#'}}">
                {# {{ form.icon | svgContents("w-12 h-12 md:w-16 md:h-16") | safe if form.icon }} #}
                <span>{{ form.title | safe }}</span>
              </a>
            </li>
          {%- endfor -%}
        </ul>
      </div>
    </div>

    <div>
      <div>
        <h2>Aktuelt</h2>
        <div>
          {%- for news in newsList -%}
            <a href="#">
              <span>{{ news.title | safe }}</span>
              <span>{{ news.date }}</span>
            </a>
          {%- endfor -%}
        </div>
      </div>
    </div>

    <div>
      <div>
        <h2>Kontakt oss</h2>
        <div>
          <div>
            <h3>
              <a href="#">Du kan chatte med oss</a>
            </h3>
            <p>Du møter først chatbot Frida. Du kan be Frida om å få chatte med en veileder (hverdager 09:00–15:00)</p>
            {# <svg viewBox="0 0 24 24" width="24" height="24"  aria-hidden="true" focusable="false" >
              <polygon points="0 0, 24 0, 0 24" />
            </svg> #}
          </div>
          <div>
            <h3>
              <a href="#">Du kan også ringe eller skrive til oss</a>
            </h3>
            <p>Se hvordan du kan kontakte oss på ulike måter.</p>
          </div>
        </div>
      </div>
    </div>

  </main>
{%- endblock -%}

{%- block styles -%}{%- endblock -%}
