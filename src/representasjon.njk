---
layout: "base.njk"
title: "Fullmakter og representasjon"
loggedIn: true
pagination:
  data: users
  size: 1
  alias: user
permalink: "/{{ user.name | slugify }}/representasjon/index.html"
---

<div class="container container--prose account-selection">
  <div class="page-crumbs"><a href="/{{ users[0].name | slugify }}/minside/">Min side</a> /</div>
  <h1>{{ title }}</h1>

</div>


{# 
  <form class="agent-chooser" id="representasjon">
    <fieldset>
      <legend>Velg hvem du skal representere</legend>
      {%- for user in users %}
      <div class="account-option card">
        <div class="avatar {{ 'avatar--self' if user.type == 'self' }}">
          {{ user.icon | svgContents | safe }}
          <input type="radio" id="{{ user.name | slugify }}" name="profil" value="{{ user.name | slugify }}/minside/" {{ 'checked' if user.type == 'self' }}>
        </div>
        <label for="{{ user.name | slugify }}">
          <span>
            <strong>{{ user.name }}</strong>
            <small>{{ user.role }}</small>
          </span>
          {% if loop.first %}<span class="tag">Deg</span>{% endif %}
        </label>
      </div>
      {%- endfor %}
    </fieldset>
    <button type="submit">Velg profil</button>
  </form>
  <script>
    const agentChooser = document.getElementById('representasjon');
    agentChooser.addEventListener('submit', function(event) {
      event.preventDefault();
      window.location.href = "{{ '/' | htmlBaseUrl }}" + agentChooser.elements["profil"].value;
    });
  </script> 
 #}
